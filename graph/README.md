# Usage graph

`usage-graph.json` is **generated** by `doqs/scripts/build_graph.py` and committed as a snapshot of which parents and builds reference each module.

```powershell
python doqs/scripts/build_graph.py
```
