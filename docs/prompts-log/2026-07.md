# Prompts log — 2026-07

---

### 09:34 — Update doqs submodule

**Prompt:** "Update the doqs submodule to the latest version."

**Actions:**

- Ran `git submodule update --init --remote doqs` to fetch and checkout latest `main`.
- Verified OKH validator after update.

**Files changed:**

- `doqs` — submodule pointer updated (`c58a038` → `3e32119`).

**Outcome:** doqs submodule is on latest main; parent repo has unstaged submodule pointer change ready to commit.
---

### 09:42 — Install FreeCAD agent skill

**Prompt:** "Ok execute the steps you recommended."

**Actions:**

- Created junction `.cursor/skills/freecad` → `doqs/skills/freecad` (symlink required admin on Windows).
- Merged `doqs/templates/cursor-rule-freecad.mdc` into `.cursor/rules/freecad.mdc`.
- Updated `.cursor/README.md` to document the skill and rule.

**Files changed:**

- `.cursor/skills/freecad/` — junction to doqs skill (SKILL.md, reference.md).
- `.cursor/rules/freecad.mdc` — modified (debugging workflow + expanded globs).
- `.cursor/README.md` — modified.

**Outcome:** Cursor can now discover and apply the FreeCAD debugging skill when working on CAD/FCStd paths.
---
