# Prompts log — 2026-08

---

### 11:05 — Diagnose CalculiX FEM material/mesh mismatch

**Prompt:** "Figure out FEM/CalculiX femelement_table error on test-fem Assembly_model and give a linked Assembly→solved FEM workflow."

**Actions:**

- Inspected `cad/models/test-fem` FCStd tree (Assembly link → BooleanFragments CompSolid → CompoundFilter → Analysis)
- Reproduced material coverage gap (2706 mesh vols vs 2430 assigned); confirmed Gmsh Solid1/2/3 groups sum correctly
- Verified FreeCAD binary solid-node search undercounts CompSolid interface C3D10 elements; empty-reference material workaround restores full coverage
- Ran CalculiX on a temp copy: ccx exit 0, `.frd` produced

**Files changed:**

- `docs/prompts-log/2026-08.md` — created

**Outcome:** Root cause and GUI fix documented; CalculiX solve verified with remaining-material assignment while keeping Assembly.FCStd link.

---

---

### 11:26 — Clarify FEM group-data false ERROR after material fix

**Prompt:** "Still see femelement_table error after empty-ref material fix; recheck test-fem."

**Actions:**

- Reopened Assembly_model.FCStd: MaterialSolid empty, MaterialSolid001=Solid1, WindowFrom=0
- Confirmed FEMMeshGmsh.inp elsets 1910+796=2706
- Explained group-data ERROR is expected noise; geometry fallback succeeded

**Files changed:**

- (none) — diagnosis only

**Outcome:** User fix verified; remaining ERROR is harmless mesh-group miss before successful 2706/2706 assignment.

---

### 15:14 — Diagnose x-axis Params spreadsheet joint expressions

**Prompt:** "Check x-axis.FCStd; linear-guide-x joints broken because x_axis_position from x-axis_Params cannot be read from linked spreadsheet — is that impossible, and how to fix?"

**Actions:**

- Inspected Document.xml of `x-axis.FCStd`, `linear-guide-x.FCStd`, `x-axis_Params.FCStd` (no `.FCStd` edits)
- Found Joint005 Distance expressions and App::Link to Params; Joint005 Invalid in parent
- Researched FreeCAD Expressions cross-doc linking, FindAliasReferences LinkMake pattern, AssemblyLink sync behavior

**Files changed:**

- `docs/prompts-log/2026-08.md` — modified

**Outcome:** Linked spreadsheets work; failure is wrong expression syntax plus AssemblyLink not bringing the Params Link into the parent.

---

### 15:38 — Top-level Params architecture for Assembly Distance

**Prompt:** "Previous fixes failed; want top-level params file driving child joints without circular deps when inserting into x-axis — how to make x-axis_Params pattern work?"

**Actions:**

- Re-analyzed AssemblyLink::synchronizeJoints (copies Distance float; does not bring child's Params Link)
- Cross-checked FreeCAD/devtalk master-spreadsheet DAG pattern and DOQS skeleton/params guidance
- Proposed concrete file topology: Params leaf + LinkMake into each consumer + machine Distance owned by x-axis

**Files changed:**

- `docs/prompts-log/2026-08.md` — modified

**Outcome:** Documented a cycle-free Params → child/parent workflow that matches FreeCAD AssemblyLink limits.

---

### 10:04 — Explore .cursor agent setup

**Prompt:** "Explore the /workspace repository's .cursor folder structure and any references to agent setup, submodules, refaqt-agents, CLAUDE.md, or .claude."

**Actions:**

- Mapped `.cursor/` tree (rules, skills, bootstrap stubs, README).
- Read key rules, skills, `AGENTS.md`, `docs/skills.md`, and initialized `doqs/` submodule to read `doqs/docs/agent-guide.md`.
- Searched repo for `refaqt-agents`, `CLAUDE.md`, and `.claude` (none found).

**Files changed:**

- `docs/prompts-log/2026-08.md` — modified

**Outcome:** Reported current agent instruction layout and DOQS submodule integration patterns.

---

### 10:52 — Install refaqt-agents submodule

**Prompt:** "Install the refaqt/refaqt-agents submodule in this repository and make sure that CLAUDE.md, AGENTS.md, and the .cursor and .claude-folder are set up to work with it."

**Actions:**

- Added `.agents/` Git submodule (refaqt-agents).
- Created `.agents-local/` with patterns, prompts-log, and DOQS skill symlinks.
- Rewrote `AGENTS.md`; created `CLAUDE.md` with `@AGENTS.md` import.
- Refactored `.cursor/rules/` to thin adapters; removed duplicated `.cursor/skills/`.
- Created `.claude/README.md` and skill symlinks.
- Updated onboarding, README, `docs/skills.md` pointer, and ADR.
- Ran submodule init, symlink checks, and doqs validators.

**Files changed:**

- `.agents/` — added (submodule)
- `.gitmodules` — modified
- `.agents-local/**` — created
- `AGENTS.md`, `CLAUDE.md` — modified / created
- `.cursor/rules/*.mdc`, `.cursor/README.md` — modified
- `.cursor/skills/**` — deleted
- `.claude/**` — created
- `docs/skills.md`, `docs/onboarding.md`, `README.md` — modified
- `docs/decisions/2026-08-28_adopt-refaqt-agents.md` — created
- `docs/prompts-log/2026-08.md` — modified

**Outcome:** Qarve now uses the three-layer refaqt-agents kit with Cursor and Claude Code adapters.

---

### 11:24 — Taiwanese ATC spindle supplier research

**Prompt:** "Find Taiwanese suppliers for spindles. Requirements: Automatic tool changer with ISO20 or larger or HSK; Power > 1.1kW; Min speed < 3000 rpm; Max speed > 23000 rpm; Torque > 0.94 Nm"

**Actions:**

- Searched public catalogs and datasheets for Taiwan spindle manufacturers (Parfaite, Theta, MicroLab, Anderson, Spintrue, Kenturn).
- Mapped published models against power, speed range, torque, taper, and ATC capability.
- Flagged gaps where min speed or torque are not published online.
- Full results: `docs/dev-log/2026-08-28_taiwanese-atc-spindle-suppliers.md`

**Files changed:**

- `docs/prompts-log/2026-08.md` — modified
- `docs/dev-log/2026-08-28_taiwanese-atc-spindle-suppliers.md` — created (follow-up entry)

**Outcome:** Identified several Taiwan suppliers with candidate ATC spindle models; min speed below 3000 rpm is the hardest requirement to verify from public ISO20/HSK datasheets.

---

### 11:46 — Log Taiwanese spindle supplier research

**Prompt:** "Can you add these results to the log?"

**Actions:**

- Created dev-log entry with full supplier tables, contact priority, RFQ questions, and open items.
- Updated prompts-log cross-reference.

**Files changed:**

- `docs/dev-log/2026-08-28_taiwanese-atc-spindle-suppliers.md` — created
- `docs/prompts-log/2026-08.md` — modified

**Outcome:** Spindle supplier research is recorded in the dev-log for follow-up RFQ and selection work.

---
