# Prompts log — 2026-06

---
### — Repository bootstrap

**Prompt:** Build repository and rules/skills from doqs_architecture.md; merge relevant `.cursor` kit.

**Actions:**
- Scaffolded DOQS module layout, doqs tooling, example modules, agent rules.

**Files changed:**
- Repository root structure, `.cursor/rules/`, `docs/`, `doqs/`, `modules/`.

**Outcome:** Initial DOQS-compliant scaffold ready for org URLs, CAD, and CI.
---

---
### — Split DOQS tools and submodule wiring

**Prompt:** Confirm qarve as machine repo; use refaqt/qarve and refaqt/doqs; Refaqt BV licensor; move architecture to doqs; OTRL/ODRL in doqs.

**Actions:**
- Published tools and docs to github.com/refaqt/doqs; submodule at qarve/doqs.
- Updated URLs, OKH licensor, agent rules to read doqs/docs/*.

**Files changed:**
- `doqs/` — submodule; qarve metadata and `.cursor` rules.

**Outcome:** Machine repo pins DOQS spec via submodule; agents use `.cursor` plus `doqs/docs/architecture.md`.
---

---
### — Single top-level module

**Prompt:** Implement plan: Qarve as single top-level module; keep `doqs` submodule; remove example `modules/frame` and `modules/x-axis`; reserve `modules/` for future sub-assemblies.

**Actions:**

- Deleted `modules/frame` and `modules/x-axis` scaffolds; rewrote `modules/README.md`.
- Removed `[[hasComponent]]` from root `okh.toml`; simplified `architecture/machine.sysml` and build lockfiles.
- Regenerated `graph/usage-graph.json`; updated README, onboarding, architecture, CONTRIBUTING, AGENTS, skills, CHANGELOG, cursor rules, and related READMEs.
- Added ADR-002 in `docs/decisions/2026-06-19_single-top-level-module.md`.
- Initialized `doqs/` submodule and ran all four validators.

**Files changed:**

- `modules/frame/`, `modules/x-axis/` — deleted
- `modules/README.md`, `okh.toml`, `architecture/machine.sysml`, `builds/*.toml`, `graph/usage-graph.json` — modified
- `README.md`, `docs/architecture.md`, `docs/onboarding.md`, `CONTRIBUTING.md`, `AGENTS.md`, `docs/skills.md`, `CHANGELOG.md`, `.cursor/rules/*.mdc`, `builds/README.md`, `cad/README.md`, `bom/README.md`, `manufacturing/notes/assembly-guide.md` — modified
- `docs/decisions/2026-06-19_single-top-level-module.md` — created

**Outcome:** Qarve operates as one top-level module with an empty `modules/` placeholder; `doqs/` submodule unchanged and validators pass.
---

---
### — Commit single-module refactor and spindle CAD

**Prompt:** "Check the changes that have been made in the qarve repo. Update all the relevant files. Then commit and push changes to main."

**Actions:**

- Reviewed single top-level module refactor; removed empty `architecture/machine.md`.
- Documented spindle CAD in `cad/README.md`, `okh.toml` source paths, assembly guide, and CHANGELOG.
- Normalized stale `modules/x-axis` references in build template and `known-consumers.toml`.
- Restored clean `doqs/` submodule; ran validators (all pass).

**Files changed:**

- `architecture/machine.md` — deleted
- `cad/README.md`, `okh.toml`, `CHANGELOG.md`, `docs/skills.md`, `manufacturing/notes/assembly-guide.md`, `builds/example-baseline.toml`, `known-consumers.toml` — modified
- `cad/assemblies/`, `cad/parts/`, `docs/decisions/2026-06-19_single-top-level-module.md` — added (with prior refactor)
- `modules/frame/`, `modules/x-axis/` — deleted (prior refactor)

**Outcome:** Single-module layout and spindle CAD committed to `main` with docs and manifests aligned.
---
