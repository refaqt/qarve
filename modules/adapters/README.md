# Adapter modules

Modules that translate between **interface versions** (e.g. spindle mount v1 → v2). Same layout as any other module; declare both `[[provides-interface]]` and `[[consumes-interface]]` in `okh.toml`.

Browse this folder for supported upgrade paths on deployed machines.
