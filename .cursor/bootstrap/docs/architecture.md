# Architecture

Copy to `docs/architecture.md` if missing. For DOQS projects, prefer the full template in the repository `docs/architecture.md` after bootstrap.

## Purpose

Modular open-hardware machine (DOQS).

## Repository layout

See `docs/architecture.md` and `architecture/*.sysml` in the target repo.
